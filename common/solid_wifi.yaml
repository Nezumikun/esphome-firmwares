wifi:
  networks:
    - ssid: !secret wifi_ssid
      password: !secret wifi_password
    - ssid: !secret solid_ssid
      eap:
        username: !secret solid_eap_user
        password: !secret solid_eap_password
